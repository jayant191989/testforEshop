@model DateTime?
@Html.TextBox("", (Model.HasValue ? Model.Value.ToString("dddd, MMMM d, yyyy") : string.Empty), new { @class = "datetimepicker6 form-control" })

<script type="text/javascript">
    $(document).ready(function () {
        $('.datetimepicker6').datetimepicker({
         //   format: 'LT',
            format: 'YYYY/MM/DD',
            //daysOfWeekDisabled: [0, 6]
        });
    });
</script>
